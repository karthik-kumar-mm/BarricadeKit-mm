import XCTest
@testable import BarricadeKit_master

final class BarricadeKit_masterTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BarricadeKit_master().text, "Hello, World!")
    }
}
